// HomeVisiteur.tsx
import React from "react";
import { Outlet } from "react-router-dom";

const RouteVisiteur = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default RouteVisiteur;
